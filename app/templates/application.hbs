{{#each @model as |todo|}}
  {{#if todo.important}}
    <ImportantTodoItem @todo={{todo}} @onMarkAsDone={{this.markAsDone}} />
  {{else}}
    <NormalTodoItem @todo={{todo}} @onMarkAsDone={{this.markAsDone}} />
  {{/if}}
{{/each}}