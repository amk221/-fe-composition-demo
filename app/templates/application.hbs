{{#each @model as |todo|}}
  {{#if todo.important}}
    <ImportantTodoItem @todo={{todo}} @onMarkAsDone={{this.markAsDone}} />
  {{else}}
    <TodoItem @todo={{todo}} @onMarkAsDone={{this.markAsDone}} />
  {{/if}}
{{/each}}